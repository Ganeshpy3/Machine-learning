# Logistic Regression

## loss function
  `loss=-ylog(ypred)-(1-y)log(1-ypred)`


## cost function 
   ` cost=-1/m Σ [ylog(ypred)+(1-y)log(1-ypred)]`
   
   
  dcost/dw=(A-Y) X
  dcost/db= (A-Y)
    
